<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.F1"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAAeV3nhy3JIJ0bmzuy8z7Mz32uhS3c5Xo" />


        <activity
            android:name=".GPmap6"
            android:exported="false"
            android:label="@string/title_activity_gpmap6" />
        <activity
            android:name=".GPmap5"
            android:exported="false"
            android:label="@string/title_activity_gpmap5" />
        <activity
            android:name=".GPmap4"
            android:exported="false"
            android:label="@string/title_activity_gpmap4" />
        <activity
            android:name=".GPmap3"
            android:exported="false"
            android:label="@string/title_activity_gpmap3" />
        <activity
            android:name=".GPmap2"
            android:exported="false"
            android:label="@string/title_activity_gpmap2" />
        <activity
            android:name=".GPmap"
            android:exported="false"
            android:label="@string/title_activity_gpmap" />
        <activity
            android:name=".grprselectivity"
            android:exported="false" />
        <activity
            android:name=".SecondPageActual"
            android:exported="false" />
        <activity
            android:name=".thrd"
            android:exported="false" />
        <activity
            android:name=".secpage"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>


</manifest>